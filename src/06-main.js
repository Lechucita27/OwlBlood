// Bootstrap: window.onload

// ============================================================
//  ARRANQUE
// ============================================================
window.onload=()=>{ const g=new Game(document.getElementById("gameCanvas")); window.__game=g; g.start(); };
